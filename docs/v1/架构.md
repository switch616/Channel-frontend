src/
│
├── api/                         # 后端接口封装（分模块）
│   ├── index.js                 # Axios 实例配置（拦截器等）
│   ├── auth.js                  # 登录注册、用户认证相关接口
│   ├── user.js                  # 用户信息、个人中心
│   ├── video.js                 # 视频上传、播放、列表
│   ├── comment.js               # 评论/回复接口
│   ├── interaction.js           # 点赞、收藏等接口
│   ├── history.js               # 观看记录
│   └── upload.js                # 视频上传专用接口（含进度）
│
├── assets/                      # 静态资源（图片、图标、视频占位图等）
│   └── icons/
│   └── images/
│
├── components/                  # 通用可复用组件
│   ├── layout/                  # 页面结构（Header、Footer、Sidebar）
│   ├── video/                   # 视频组件（卡片、播放器等）
│   ├── user/                    # 用户组件（头像、信息卡、关注按钮等）
│   ├── comment/                 # 评论组件（列表、输入框等）
│   ├── interaction/             # 点赞、收藏等交互组件
│   ├── modal/                   # 通用弹窗组件
│   ├── test/                    # 测试组件
│   └── common/                  # 按钮、输入框、加载状态等原子组件
│
├── views/                       # 页面视图（按业务模块拆分）
│   ├── auth/                    # 登录、注册、找回密码
│   │   └── Login.vue
│   │   └── Register.vue
│   │   └── ForgotPassword.vue
│   ├── home/                    # 首页（推荐视频、分类等）
│   │   └── Home.vue
│   ├── video/                   # 视频播放页、上传页
│   │   └── VideoPlayer.vue
│   │   └── Upload.vue
│   ├── user/                    # 用户中心、我点赞的视频、我上传的内容
│   │   └── Profile.vue
│   │   └── MyVideos.vue
│   │   └── MyLikes.vue
│   │   └── MyCollections.vue
│   │   └── History.vue
│   └── not-found/               # 404 页面
│       └── NotFound.vue
│
├── router/                      # Vue Router 配置
│   ├── index.js        # 创建 router 实例，注册全局守卫
│   ├── routes.js       # 路由表定义，支持懒加载和模块化
│   └── guards.js       # 路由守卫（权限验证、token 过期拦截）
│
├── stores/                      # Pinia 状态管理
│   ├── user.js                  # 用户信息状态（token、权限）
│   ├── video.js                 # 当前视频状态、列表缓存等
│   ├── interaction.js           # 点赞/收藏状态
│   ├── comment.js               # 评论数据缓存
│   └── global.js                # 全局状态，如加载框、弹窗控制
│
├── utils/                       # 工具函数
│   ├── auth.js                  # 权限判断、登录跳转等
│   ├── format.js                # 时间格式化、数字转化等
│   ├── validator.js             # 表单校验函数
│   └── storage.js               # localStorage/sessionStorage 操作封装
│
├── styles/                      # 全局样式（可支持 SCSS）
│   ├── reset.css                # CSS Reset
│   ├── variables.scss           # SCSS 变量
│   ├── global.scss              # 全局样式
│   ├── style.css                # 基础样式
│   └── theme/                   # 可选主题支持
│
├── App.vue                      # 根组件
└── main.js                      # 入口文件（初始化 router、store、UI 框架等）

# 项目根目录
├── public/                      # 静态资源目录（不需要经过构建工具处理的文件）
│   └── favicon.ico             # 网站图标
│
├── docs/                        # 项目文档
│   └── v1/                     # 文档版本
│       └── 架构.md             # 项目架构说明
│
├── .vscode/                     # VS Code 配置
│   └── settings.json           # 编辑器配置
│
├── vite.config.js              # Vite 构建工具配置
├── index.html                  # HTML 模板
├── package.json                # 项目依赖和脚本配置
├── pnpm-lock.yaml             # pnpm 依赖锁定文件
├── .gitignore                 # Git 忽略文件配置
└── README.md                  # 项目说明文档
