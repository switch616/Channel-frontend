熟悉我的前后端代码，目前我有一些需求如下：
1、页面规范性，对于某些路由，目前是比如http://localhost:5173/video/8，代表第八个视频？？，表设计应该给每个视频或者什么评论或者文章都有一个唯一中长id,路由显示应该是比如说video/as91s12s这种
2、接口规范安全：现在的某些新增或者删除接口，甚至是比如“
http://localhost:8088/api/v1/comment/12”这样就行？，应该把删除的对应id作为参数传递？还是说大厂都是这个规范的，这只是一个示例，实际上接口都要调整规范
3、排查全部代码规范并调整
